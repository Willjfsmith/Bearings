{
  "name": "bearings",
  "version": "0.2.0",
  "private": true,
  "description": "Flags, maps and capitals, with the tricks to remember them.",
  "scripts": {
    "dev": "python3 -m http.server 8000",
    "check": "node --check js/app.js && for f in js/data/*.js sw.js; do node --check \"$f\" || exit 1; done"
  }
}
